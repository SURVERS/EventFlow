# 🎯 EventFlow – Система управления событиями

**EventFlow** – это комплексное решение для управления мероприятиями, состоящее из высокопроизводительного RESTful API на Go и современной административной панели на React. Система предназначена для организаторов, которым нужен полный контроль над событиями, участниками и аналитикой.

---

## 🚀 Ключевые возможности

*   **📅 Управление событиями** – Полный цикл: создание, планирование, публикация
*   **👥 Управление участниками** – Регистрация, билеты, проверка на входе
*   **🎫 Билеты с QR-кодами** – Генерация и валидация цифровых билетов
*   **📊 Аналитика в реальном времени** – Дашборд со статистикой и графиками
*   **🔐 Безопасность** – JWT-аутентификация с refresh-токенами
*   **⚙️ Полноценный CRUD** – Управление всеми сущностями через интуитивный интерфейс

---

## 🏗️ Архитектура системы

### **Backend** (Go)
- **Фреймворк**: Gin – высокопроизводительный веб-фреймворк
- **База данных**: PostgreSQL с использованием GORM ORM
- **Аутентификация**: JWT (access + refresh tokens)
- **Документация API**: Swagger/OpenAPI 3.0
- **Миграции**: golang-migrate
- **CORS**: Настроен для кросс-доменных запросов

### **Frontend** (React Admin)
- **Фреймворк**: React 19 с TypeScript
- **UI библиотека**: React-Admin (на основе Material-UI)
- **Сборщик**: Vite (rolldown-vite) для быстрой разработки
- **Визуализация**: Recharts для графиков и диаграмм
- **QR-коды**: Встроенная генерация QR-кодов для билетов

---

## 📊 Модель данных

Система построена вокруг следующих основных сущностей:

| Сущность | Описание |
|----------|----------|
| **Категории** (`categories`) | Классификация событий по темам |
| **Типы событий** (`event_types`) | Типология мероприятий (конференция, мастер-класс и т.д.) |
| **События** (`events`) | Основная сущность с настройками времени, статусом публикации |
| **Участники** (`participants`) | Посетители мероприятий с контактными данными |
| **Организаторы** (`organizers`) | Пользователи системы с ролями (admin/organizer) |
| **Регистрации** (`event_registrations`) | Связь между участниками и событиями |
| **Билеты** (`tickets`) | Цифровые билеты с уникальными QR-кодами |

---

## 🔧 Функциональность API

### Основные группы эндпоинтов:
- **CRUD операции** для всех сущностей системы
- **Аутентификация** – регистрация, вход, обновление токенов
- **Статистика** – дашборд, аналитика событий, участников
- **QR-коды** – генерация и валидация билетов
- **Защищенные маршруты** – JWT middleware для контроля доступа

> 📚 Полная документация API доступна через Swagger UI по адресу `/swagger/`

---

## 🖥️ Административная панель

React-Admin панель предоставляет:

- **Интуитивный интерфейс** для управления всеми данными
- **Дашборд** с ключевыми метриками и визуализациями
- **Гибкая система аутентификации**
- **CRUD операции** для каждой сущности через удобные формы
- **Адаптивный дизайн**, работающий на любых устройствах

---

## 🚀 Быстрый старт

### Требования:
- Go 1.21+
- PostgreSQL 12+
- Node.js 18+

### Установка:
```bash
# Бэкенд
cd backend
go mod download
go run main.go

# Фронтенд
cd frontend
npm install
npm run dev
